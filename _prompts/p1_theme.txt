任务:为 tkinter/ttk 程序编写一个完整的暗色主题模块文件 `gui_theme.py`(Python 3.10+, Windows, Tk 9.0 / ttk)。输出 ONLY 该文件的完整代码,不要任何解释、不要 markdown 围栏。文件用 UTF-8,第一行写 `# -*- coding: utf-8 -*-`。

该模块将被 `gbfr_gui.py` 导入,提供以下固定 API(必须全部实现,签名不得改变):

```python
# ---- 颜色常量(只能使用 6 位十六进制,Tk 不支持 8 位带透明度的颜色) ----
BG          # 窗口底色,深海军蓝 #0d1b2c
BG_PANEL    # 面板/卡片背景 #1b2636
BG_INPUT    # 输入框/下拉背景 #0f1d2e
BG_TEXT     # 日志/文本区背景 #101418
FG          # 主文字 #e8ecf1
FG_DIM      # 次要文字 #8b98a9
FG_FAINT    # 更淡文字 #5c6b7f
ACCENT      # 主青色 #67e8f9
ACCENT_DARK # 青色深一号 #22b8d4
GREEN       # 成功绿 #4ade80
RED         # 危险红 #f87171
AMBER       # 警告黄 #fbbf24
PURPLE      # 信息紫 #a5b4fc
BORDER      # 边框 #2e4057
BORDER_LT   # 亮边框 #3b5371
FONT_FAMILY     = "Microsoft YaHei UI"   # 界面字体
FONT_FAMILY_MONO = "Consolas"            # 等宽字体

def apply_theme(root): ...
    # 1) root.configure(bg=BG);对 root 用 option_add 设置全局默认:
    #    *Font 为 (FONT_FAMILY, 10) —— 注意 ttk 里用 ttk.Style().configure('.', font=...) 更可靠,两者都做。
    # 2) 构造 ttk.Style()(theme_use('clam') 作为基础,clam 才支持完整配色),
    #    然后 configure/map 以下样式,保证暗色统一、文字可读:
    #    '.'、'TFrame'、'TLabel'(fg=FG, bg 透明继承)、'TButton'、'Accent.TButton'(青色主按钮,白字)、
    #    'Green.TButton'、'Red.TButton'、'Danger.TButton'(红底)、'TEntry'(BG_INPUT 底、FG 字、BORDER 边框、
    #    聚焦时 ACCENT 边框,通过 map 的 focus 状态)、'TCombobox'(含 arrowcolor、fieldbackground)、
    #    'TCheckbutton'(fg=FG、activebackground 继承、indicator 边框)、'TLabelframe'(BG_PANEL 底+BORDER 边框)、
    #    'TLabelframe.Label'(fg=ACCENT)、'TNotebook'(底 BG,borderwidth=0)、'TNotebook.Tab'(fg=FG_DIM、
    #    底 #182636;selected 时底 #1b2636、fg ACCENT、加 ACCENT 下划线效果用 border)、
    #    'Vertical.TScrollbar'/'Horizontal.TScrollbar'(troughcolor=BG, 滑块 #33415c, hover 更亮)、
    #    'TProgressbar'(青色)、'TEntry' 的 readonly 等状态也要 map。
    # 3) 用 root.option_add 配置 Combobox 弹出列表配色:
    #    '*TCombobox*Listbox.background' = BG_INPUT、'*TCombobox*Listbox.foreground' = FG、
    #    '*TCombobox*Listbox.selectBackground' = 深青色 #0e3a4a、'*TCombobox*Listbox.selectForeground' = ACCENT、
    #    '*TCombobox*Listbox.font' = (FONT_FAMILY, 10)。
    # 4) 菜单/对话框配色:option_add '*Menu.background' 等,让右键菜单和文件对话框内的菜单也暗色(菜单用
    #    '#1b2636' 底、'#e8ecf1' 字、activeBackground '#22b8d4')。
    # 注意:ttk.Button 在部分状态(disabled)需要 map background/foreground,保证禁用时变暗可辨识。

def emit_line(text_widget, line): ...
    # 把一行文本插入 text_widget(已处于 normal 状态),并按行首标记自动选择颜色 tag:
    #   行内含 '[错误]'  -> 'err'   (RED)
    #   行内含 '[警告]'  -> 'warn'  (AMBER)
    #   行内含 '[完成]'  -> 'done'  (GREEN)
    #   行内含 '[预览]'  -> 'preview' (PURPLE)
    #   行内含 '[信息]' 或 '[物品]' 或 '[任务]' 或 '[奖励]' -> 'info' (ACCENT)
    #   其余(纯输出行,如列表结果) -> 默认前景 FG
    # 方法:先用 configure_tags(text_widget) 确保 tag 存在,再 insert("end", line, tag),
    # 最后 see("end")。tag 配置:
    #   err=RED / warn=AMBER / done=GREEN / preview=PURPLE / info=ACCENT(全用 6 位 hex)。

def configure_tags(text_widget): ...
    # 幂等地为 text_widget 配置上述 tag(带 exist_ok 保护)。

def make_icon(): ...
    # 纯 tkinter 程序化绘制一个 64x64 青色菱形宝石图标,返回 tk.PhotoImage:
    #   用 PhotoImage(width=64, height=64) + put() 逐像素/矩形填充。
    #   设计:深色底透明(不填充,保持透明),中央一个由 4 条斜边组成的菱形(近似宝石切面):
    #   顶角亮青色 #a5f3fc,主体 #67e8f9,底部深青 #22b8d4,左右加 #3dd5f5 高光边,底部一条 #155e75 阴影。
    #   菱形顶点约 (32,8) (56,32) (32,56) (8,32),内部用逐行扫描把在菱形内的像素着色,
    #   上 1/3 用亮色、中 1/3 用主色、下 1/3 用深色,做出渐变感;菱形边缘外留 1-2px 透明。
    #   返回的 PhotoImage 引用必须由调用方持有(全局引用防 GC)。

def font(size=10, bold=False): ...
    # 返回 (FONT_FAMILY, size, 'bold' if bold else 'normal')

def mono_font(size=10): ...
    # 返回 (FONT_FAMILY_MONO, size)
```

硬性要求:
- 不要导入任何第三方库,只用 tkinter/ttk/标准库。
- 所有颜色一律 6 位 hex;所有控件确保暗色下文字对比度足够。
- ttk.Style 的 configure 与 map 要覆盖 focus/active/disabled/selected 等关键状态,避免出现白底白字或黑字黑底。
- 代码要能在 Python 3.10~3.14 与 Tk 8.6/9.0 下运行;不要在模块顶层创建 Tk() 实例(只定义常量与函数)。
- 模块顶部写简短 docstring 说明用途。总行数控制在 420 行以内,风格紧凑清晰。
